package smilebot.model;

public enum CustomFields {

    // NO info, so exception will be trow
    NONE,

    // General
    ID,

    // Channel
    CHANNEL_NAME,

    //Thread
    THREAD_NAME,
    THREAD_ARCHIVED,

    // Emoji
    EMOJI_NAME
}
